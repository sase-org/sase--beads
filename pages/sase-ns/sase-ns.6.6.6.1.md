# Bead: sase-ns.6.6.6.1 — Isolate the process-global merged-config cache so its nodes stop failing the flake gate

[Bead Pages](../README.md) / [sase-ns.6.6.6](sase-ns.6.6.6.md) / sase-ns.6.6.6.1

**Status:** ✓ closed · **Resolution:** done · **Type:** ↳ phase
**Owner:** `bryanbugyi34@gmail.com` · **Created by:** [bbugyi200.athena.sase-ns.6.6.land--1](https://github.com/sase-org/sase--agents/blob/main/families/bbugyi200.athena.sase-ns.6.6.land.md) · **Assignee:** `sase-ns.6.6.6.1` · **Size:** large
**Created:** 2026-08-17 05:54:39 EDT · **Closed:** 2026-08-17 13:13:54 EDT
**Plan:** [202608/backlog\_top\_five\_gates\_and\_flakes.md](https://github.com/sase-org/sase--plans/blob/main/202608/backlog_top_five_gates_and_flakes.md)

## Description

configcache: fix the process-global merged-config cache leak behind tests/test_config_cache.py::test_selector_change_eventually_invalidates_merged_config, the only non-epic-owned node still holding `just selection-health --fail-on-new-flake` red, and verify its sibling nodes in the same file.

## Notes

[2026-08-17T10:29:08Z · sase-ns.6.6.6.1] PROGRESS: Implemented atomic config-generation publication (lock around token/hit, rebuild off-lock, CAS publish) and test-only publisher restriction after each cache clear. Added leftover-loader and stale-rebuild regressions. Isolation + file-scoped SASE_CONTENTION_REPEAT=3 contention are green (27 passed x3). Next: just check-full via monitor (conftest_runtime is in the broadening set), then selection-health and close.

[2026-08-17T11:14:19Z · sase-ns.6.6.6.1--2] PROGRESS: First-fill coalesce is in _can_install_config_cache_slot (empty slot may publish; replacement/refresh still test-thread-only). Extracted cache helpers to tests/_config_cache_helpers.py so tests/test_config_cache.py is 964/1000 (toobig was 1021). just lint green; just test tests/test_config_cache.py tests/test_config_cache_isolation.py: 36 passed. Next: just check-full via monitor to prove config_load_merged is back near 18k-20k (broken first-fill was 32810).

[2026-08-17T13:32:58Z · sase-ns.6.6.6.1] PROGRESS: Root-caused by measurement, no code changed yet. Instrumented every call into load_merged_config/get_agent_owner_config_snapshot/clear_config_cache/current_config_token with the calling thread name over a serial tests/ace/ + tests/test_config_cache.py run: 5121 config calls from non-main threads, from exactly two families — sase-ace-proc-observer (1413 token / 475 merged / 938 owner) and asyncio_0..6 (1563 token / 690 merged / 70 owner / 3 clear_config_cache). ProcObserver.stop() (src/sase/ace/tui/proc_observer.py:339) sets _stop then joins with timeout=1.0 and ABANDONS the daemon on timeout, so an in-flight 0.5s poll finishes inside the next test in the same xdist worker and rebuilds/replaces _merged_config_cache_value — that is what breaks `assert load_merged_config() is first`. Secondary: load_merged_config and get_agent_owner_config_snapshot publish (token, value) as two unlocked writes (torn read + stale clobber), and _refresh_current_config_token clears _current_config_token_refresh_thread unconditionally so a stale-epoch leftover deregisters the live worker (explains the single-flight node). RULED OUT: the sase-loader ThreadPoolExecutor leaks across 3884 consecutive test boundaries but its workers made zero config calls. Tale plan sase_plan_config_cache_publication_isolation.md authored and validated; proposing now.

[2026-08-17T14:23:38Z · sase-mv--1] DISCOVERED ISSUE: the approved sase-mv tale (config_cache_ambient_reader.md) took step-2 branch 1 for sase-ace-proc-observer and left the asyncio_* Textual-worker family you already inventoried. Full-lane probe qdw2tbd135ka: 29852 ambient calls, 520 poisoning reads, timed_out_refresh_workers=0. Proc-observer drain is now in tests/_conftest_runtime.py via stop_orphaned_proc_observers(); AcePage failed-enter no longer leaks. asyncio_* stacks still go through llm_override_indicator.build_launch_model_setting_snapshot -> get_llm_provider_config. Not filing a separate task — this phase already named that family.

[2026-08-17T15:26:32Z · sase-mv--2] sase-mv closed. Step-2 branch 1 (proc-observer leak) + two consecutive green full pytest lanes (just check escalated 20260817T143226Z-5abf9eb64e3c-305699; check-full pytest br9zpse8a1qq / 20260817T144608Z-5abf9eb64e3c-620023, 32037 passed). just check-full recipe still red only on sase-j0 cost budgets (corroborated +19). selection-health still names the selector-change node from historical pre-fix records (sase-nv), plus sase-ob and sase-oh; both post-fix full lanes passed that node.

[2026-08-17T16:33:33Z · sase-ns.6.6.6.1] PROGRESS: Re-verified phase state on master c4a29f213 after 2959d3992 landed sase-mv step-2 branch 1. Gate is clean for this class: just selection-health --fail-on-new-flake now names only tests/test_models_panel_edit_outcomes.py::test_on_alias_edited_offers_commit_when_in_repo (task bead sase-oh); the selector node is excluded only as "no longer collectable" because c715bacbc split it into tests/test_config_cache_selector.py, not by a declared fix. Four consecutive zero-failure full-run records at/after the fix (20260817T143226Z, T144608Z, T153355Z at the fix commit with a clean tree, T160823Z). Remaining, unfixed on master: (1) load_merged_config/get_agent_owner_config_snapshot still publish (token, value) as two unlocked stores (src/sase/config/core.py:547-559, 628-658); (2) _refresh_current_config_token clears _current_config_token_refresh_thread unconditionally, outside the epoch guard (core.py:167), so a stale worker deregisters the live one; (3) the asyncio_* Textual thread-worker ambient config readers are still unmeasured post-fix; (4) no # fixed-at: block retires this class pre-fix evidence, and eight still-recorded node IDs carry records inside the gate window. Authored tale plan sase_plan_config_cache_atomic_publication.md covering all four; validated, proposing now.

[2026-08-17T17:10:44Z · sase-ns.6.6.6.1] Step 3 re-measurement (plan 202608/config_cache_atomic_publication.md), full lane: `.venv/bin/python -m pytest -n 2 -p tests._config_reader_probe --sase-detect-config-readers tests/ace/tui tests/test_config_cache.py tests/test_config_cache_token.py tests/test_config_cache_selector.py tests/test_config_cache_teardown.py tests/test_config_cache_isolation.py tests/test_llm_override_indicator.py` — 9621 passed, 0 failed. Probe: 4207 ambient read(s) from 181 record(s); 150 poisoning; 33 cross-test thread(s); 0 refresh worker(s) still live after origin.

Breaking down the 150 poisoning records: 139 are the asyncio_0..8 Textual/asyncio default-executor thread pool (22 through sase.ace.tui.widgets.llm_override_indicator's run_worker(thread=True) default-resolution path exactly as sase-mv's step-1 probe already named; 117 through a second, previously-unrecorded call site -- asyncio.to_thread(prepare_loaded_agents_worker_boundary, ...) in src/sase/ace/tui/actions/agents/_loading_disk.py (both the async_load and artifact_delta_load paths) calling get_max_running_agents() -> load_merged_config() from _loading_compute.py:404). The remaining 11 are a probe self-artifact, not a real leak: they carry thread names like "Thread-423 (load_merged_config)" that exactly match this plan's own new generation-guard regression tests (test_load_merged_config_discards_build_invalidated_by_concurrent_clear, test_owner_config_snapshot_discards_build_invalidated_by_concurrent_clear, and the token single-flight lambdas in test_config_cache_token.py), whose builder threads are created and joined entirely inside their own test. tests/_config_reader_probe.py keys self._threads purely by native thread_ident and never clears a stale entry (_remember_thread_locked's `if ident in self._threads: return`), so when the OS reuses a just-freed native thread id for one of these short-lived test threads, the probe attributes its origin to whichever unrelated earlier test first used that recycled ident. Confirmed by cross-referencing originating_nodeid/poisoning_nodeids pairs in .pytest_cache/sase-config-readers.json; not filing a task bead for this probe nuance per this step's 'do not create a bead' instruction, recording it here for context on why the raw 150 count overstates real poisoning.

Branch taken: (3) recorded, not fixed in this plan. Both real families (llm_override_indicator and the agents-loading asyncio.to_thread calls) read config through Python's asyncio default ThreadPoolExecutor, which is process-shared and outlives any single ACE app/test the same way sase-mv's original probe run already found for llm_override_indicator -- confirmed here to also cover ordinary, correctly-awaited `await asyncio.to_thread(...)` calls in the agents-loading pipeline, not just unawaited background workers. Cancelling or replacing that shared executor per-app is out of this plan's scope and risks destabilizing every other asyncio.to_thread call in ACE. PROPOSED FOLLOW-UP filed as a note here per instruction, not a bead: if this residual ambient-read count needs to shrink further, the fix belongs at the executor-sharing root (e.g., a per-app custom executor for AcePage tests, or accept the shared default and stop probing pass/fail on it), and should re-measure with this same probe command first since the mix has shifted since sase-mv's step 1 (llm_override_indicator is now the minority contributor at 22/139, not the majority).

[2026-08-17T17:13:54Z · sase-ns.6.6.6.1] Implemented plan 202608/config_cache_atomic_publication.md steps 1, 2, and 4; step 3 re-measured and recorded (branch 3, no code fix). Environment note: this workspace's linked sase-core checkout (sase/repos/linked/sase-core, HEAD 02a37e9) is one unreleased commit ahead of what tools/validate_sase_core_rs expects (agent-stats work-schema bumped 5->6), which makes _setup -- and therefore every `just check`/`just check-full`/`just lint`/`just test-scoped`/`just test-contention`/`just selection-health` invocation -- fail before running anything. This is unrelated to config-cache work (confirmed: it's a Rust/agent-stats schema probe, my diff touches only Python config-cache code and tests); filed as ready task sase-oq rather than worked here. All verification below therefore ran the underlying tools (ruff/mypy/pytest/tools/selection_health/tools/run_pytest contention) directly instead of through the blocked `just` wrappers.

STEP 1 (atomic single-slot publication + generation guard) and STEP 2 (refresh worker deregisters only itself): implemented exactly as specified in src/sase/config/core.py. `grep -rn '_merged_config_cache_token\|_merged_config_cache_value\|_agent_owner_config_cache_token\|_agent_owner_config_cache_value' src tests` -> no matches, confirming no leftover references to the old split globals.

STEP 3 (residual non-main-thread readers), full command: `.venv/bin/python -m pytest -n 2 -p tests._config_reader_probe --sase-detect-config-readers tests/ace/tui tests/test_config_cache.py tests/test_config_cache_token.py tests/test_config_cache_

… and 3909 more characters

## Agents

| Agent | Bead | Commits |
|---|---|---:|
| [bbugyi200.athena.sase-ns.6.6.6.1](https://github.com/sase-org/sase--agents/blob/main/families/bbugyi200.athena.sase-ns.6.6.6.1.md) | [sase-ns.6.6.6.1](sase-ns.6.6.6.1.md) | 0 |
